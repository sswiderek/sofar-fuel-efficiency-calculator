# API Key for OpenAI (used for fuel price predictions)
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Database connection string (if using a database)
# DATABASE_URL=postgres://username:password@localhost:5432/maritime_calculator

# Optional: Port configuration (defaults to 5000)
# PORT=5000